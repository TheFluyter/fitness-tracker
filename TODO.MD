### TODO
- link exercises to users
  - provide a solution for adding exercises a different user has already added
    - check if exercise is already in the database
    - if not, add it to the database and add it to the user
    - if it is, add the exercise to the user
  - expand exercise logs
- fix security issues
  - csrf issues
  - configure authorization
- improve error logging and error pages (reproduction for unexpected errors: change name of the error modal)
- add notification when wrong credentials

## Features
- food tracker
